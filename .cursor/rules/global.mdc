---
alwaysApply: true
---

- ファイル内に記述するコメントなどは全て英語にして下さい
- `.dev.vars`は`.env`と同じで秘匿情報が含まれているので読み込まないで下さい
- scriptの実行は`package.json`に定義されているscriptをなるべく実行して下さい。`dotenvx`や`bun --env-file`によってsecretを読み込む設定をしてあります。